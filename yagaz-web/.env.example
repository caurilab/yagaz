VITE_API_URL=https://api.yagaz.test
# true (défaut) : dashboards alimentés par des fixtures typées (src/api/fixtures.ts).
# false : bascule tous les endpoints vers de vrais appels HTTP (VITE_API_URL).
VITE_DEMO_MODE=true
